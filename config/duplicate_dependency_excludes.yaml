# Dependencies intentionally declared in both the workspace root and
# a workspace member. These will NOT be flagged as REDUNDANT by
# check_duplicate_dependencies.
#
# Self-duplicates (same dep in multiple sections of a single
# pyproject.toml) are NOT affected by this list: version-mismatch
# self-duplicates always block, same-version duplicates warn.
#
# Add entries here when a dep must stay in the root pyproject.toml
# despite also being provided by a workspace member.

excludes:
  - tqdm
