---
- name: reload systemd
  ansible.builtin.shell: systemctl daemon-reload >> /tmp/llamaserver-vulkan-ops.log 2>&1
  register: _daemon_reload
  changed_when: true
- name: Display daemon-reload result
  ansible.builtin.debug:
    var: _daemon_reload.stdout_lines