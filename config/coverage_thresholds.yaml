# WORKSPACE-VM Test Coverage Thresholds

unit:
  path: tests/unit
  min_coverage: 90
  source_path: workspace
  runner: uv run python -m pytest

integration:
  path: tests/integration
  min_coverage: 50
  source_path: workspace
  runner: uv run python -m pytest
