# Per-repo safe exceptions for sensitive file detection.
# These are merged with projects/CI/config/sensitive_files.yaml safe_exceptions.

safe_exceptions:
  - .gitattributes
  - .markdown_docs_exceptions.yaml
